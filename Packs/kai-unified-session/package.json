{
  "name": "kai-unified-session",
  "version": "0.1.0",
  "description": "Persistent goal graph and session continuity layer for PAI",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "cli": "bun run src/cli/index.ts"
  },
  "dependencies": {
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}
